<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rodendanskipodsjetnik">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ikona"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ikona"
        android:supportsRtl="true"
        android:theme="@style/Theme.RođendanskiPodsjetnik">
        <activity android:name=".DodajActivity" android:screenOrientation="portrait"></activity>
        <activity android:name=".SviActivity" android:screenOrientation="portrait"/>
        <activity android:name=".ViseActivity" android:screenOrientation="portrait" />
        <activity android:name=".MainActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".AlarmReceiver" />
    </application>

</manifest>